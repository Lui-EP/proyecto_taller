# Backend Legacy (Node) - Solo referencia

Este backend en `backend/` es la version anterior en Node.

## Estado

- Se conserva por compatibilidad y referencia.
- El backend activo del proyecto es `backend-micro/` (FastAPI).

## Si quieres ejecutar este backend legacy

```bash
cd backend
npm install
npm run dev
```

API legacy:

- `http://localhost:3000/api`

## Recomendacion

Para web y movil actuales usa siempre:

- `clientes-service` en `:8001`
- `pedidos-service` en `:8002`

Documentacion actual:

- `backend-micro/README.md`
